# 이 파일을 .env 로 복사해서 값을 채운다. .env 는 .gitignore 에 있으므로 커밋되지 않는다.
#
#     cp .env.example .env
#
# docker compose 는 같은 디렉터리의 .env 를 자동으로 읽어 컨테이너에 전달한다.
# 컨테이너 없이 돌릴 때는 셸에서 export 한다.
#
# 키가 없는 엔진은 화면에서 "(키 없음)" 으로 잠긴 채 보인다 — 조용히 결정적 구성으로
# 대체되지 않는다.

GEMINI_API_KEY=
ANTHROPIC_API_KEY=
